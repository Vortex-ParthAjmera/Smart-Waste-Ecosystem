# Local Model Artifacts

Runtime weights are intentionally ignored unless redistribution is explicitly approved. Provision the artifact referenced by `scripts/demo/ml/model-manifest.json`, verify its SHA-256, and keep raw frames out of persistent storage by default.
