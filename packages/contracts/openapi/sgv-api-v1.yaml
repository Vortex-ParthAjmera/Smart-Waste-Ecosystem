openapi: 3.1.0
info:
  title: Smart Waste Ecosystem API
  version: 1.1.0
paths:
  /api/v1/me/dashboard:
    get:
      summary: Citizen dashboard projection
      responses:
        "200":
          description: Dashboard envelope
  /api/v1/device/sync:
    post:
      summary: Authenticated edge gateway sync
      responses:
        "200":
          description: Exact-once sync result
        "409":
          description: IDEMPOTENCY_CONFLICT
